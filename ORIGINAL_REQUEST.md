# Original User Request

## Initial Request — 2026-06-27T10:18:54Z

Make the Aegis Control Plane Design (landing page and dashboard) completely premium, high-fidelity, and launch-ready using a warm-editorial style (with high-fidelity visual elements like glassmorphism, subtle grid patterns, glowing accents, and concentric card borders).

Working directory: c:\Users\Windows 10\Downloads\Aegis Control Plane Design
Integrity mode: development

## Requirements

### R1. Complete Design Polish (Landing & Dashboard)
Inspect all landing page sections and the `/app` dashboard for layout, alignment, border, and typography details that feel "cheap". Re-implement card surfaces to use concentric border radii and replace flat borders with layered shadows-as-borders for depth. Apply subtle grid patterns and glassmorphism.

### R2. High-Fidelity Micro-interactions
Ensure all transitions, hover states, menu reveals, accordion expansions, and theme toggling animate smoothly using Framer Motion and standard CSS transitions. All click actions must have tactile scaling and spring curves.

### R3. Flawsee Contrast & Accessibility
Ensure WCAG AA contrast ratio compliance (> 4.5:1) for all text on both light and dark backgrounds. Verify accessibility landmarks, skip-links, ARIA tags, and keyboard focus outlines are present and functional.

### R4. Automated Verification Tests
Write and run Playwright automated test scripts to objectively verify that the landing page and dashboard are fully functional, have no console errors, pass axe accessibility check minimums, and display correctly without layout overflow.

## Acceptance Criteria

### Visual & Interactive Quality
- [ ] All nested rounded cards/surfaces follow the concentric border-radius rule: `outerRadius = innerRadius + padding`.
- [ ] Static borders on main elements are replaced with layered shadows (`--shadow-border`) for depth.
- [ ] All clickable buttons, tags, toggles, and sidebar navigation links scale to `0.96` on click/active press.
- [ ] Theme toggles on both landing page and dashboard crossfade smoothly using spring animations (`AnimatePresence`).
- [ ] Monospace numbers, spend limits, hashes, and tabular values use `font-variant-numeric: tabular-nums` to prevent layout shift.

### Contrast & Accessibility (a11y)
- [ ] No gray-on-dark text elements have contrast ratios lower than 4.5:1 (WCAG AA).
- [ ] Skip-to-content navigation links are present and accessible via keyboard Tab.
- [ ] Dropdowns and menu buttons have correct ARIA tags (`aria-expanded`, `aria-haspopup`, `role="menu"`).

### Build & Verification
- [ ] TypeScript compilation (`tsc`) succeeds with zero errors.
- [ ] Production build (`npm run build`) packages all assets with zero errors.
- [ ] Playwright tests are written under `/tests` and run successfully, verifying page load, basic routing, and element presence.

## Follow-up — 2026-06-27T10:23:27Z

Hello, here is the detailed Visual Design Audit Report generated by the browser subagent. Please feed these specific design findings directly into the Implementation Track (Conv ID: 826a3f7d-32aa-4558-a91a-9e2fc16a849d) and the E2E Testing Track (Conv ID: b369afbd-e9c1-4d59-86db-7c6bea88308a) to ensure they are fully implemented and verified:

---

# Visual Design Audit Report Findings to Resolve

## 1. Colors & Semantic Color Palette (Critical Defect)
* Re-establish warm burgundy/crimson and soft semantic color branding. Do not collapse "--ok", "--info", "--warn", "--bad" and brand colors into flat black/white.
* Ensure status badges (ALLOW, DENY, STEP_UP, NOTICE) are color-coded appropriately rather than monochrome gray.
* Standardize action buttons ("Approve & sign", "Deny") in the Inbox to use standard red/green highlights with proper contrast.

## 2. Contrast & Accessibility (a11y)
* Increase inactive navigation link contrast to >= 4.5:1 (use at least #737373 in light mode and #b0b0b0 in dark mode).
* Fix low-contrast sub-labels in the sidebar footer and Settings page details (ensure WCAG AA 4.5:1).

## 3. Layout Alignments & Grid Consistency
* Standardize concentric card borders and inner-to-outer radii ratios (concentric nesting).
* Clean up layout alignments and viewport flex wrapping on dashboard stat cards.

## 4. Empty States
* Upgrade the "Inbox zero" empty state with a larger icon with layered opacity, soft background wireframes, and secondary suggestions (e.g. "Configure new mandates").

## 5. Tab & Hover Transitions
* Add subtle 1px translate and shadow lift (--shadow-lift) on card hover.
* Animate transitions when switching tabs (Overview, Governance, Inbox) using Framer Motion.

## 6. Modal Setup Wizard Polishes
* Highlight active indicator (.is-active) in the Wizard (use border color overlays).
* Make inputs feel premium (animated focus rings, etc.).
* Add a visual "Copied!" tooltip overlay to command copy buttons.

## 7. Collapsed Sidebar Tooltips
* Add visual tooltips when sidebar collapses below 860px screen width.

---

Please confirm receipt and verify these are integrated into the plan.

## Follow-up — 2026-06-28T04:59:14Z

Make the Aegis Control Plane Design site launch-ready as a premium portfolio/demo showcase. The site is a Vite + React + TypeScript + Framer Motion single-page app with a marketing landing page and a multi-page governance dashboard. It needs a full production-quality sweep: visual polish, responsiveness, accessibility, performance, cleanup, and bug fixes.

Working directory: c:\Users\Windows 10\Downloads\Aegis Control Plane Design
Integrity mode: benchmark

## Requirements

### R1. Visual polish and dashboard redesign
The dashboard (all pages: Overview, Governance, Inbox, History, Providers, Devices, Settings) should feel premium and state-of-the-art. The landing page and dashboard should use a cohesive monochrome (white/black/grey) design language with the crimson accent. The video background on the landing page must be clearly visible behind dashboard content while keeping text readable. Apply the design engineering skills in `.agents/skills/make-interfaces-feel-better/` and `.agents/skills/transitions-dev/` to refine animations, shadows, typography, and micro-interactions across the entire site.

### R2. Mobile responsiveness
Both the landing page and dashboard must work well on mobile viewports (320px–768px). Navigation, layout, typography, and interactive elements should adapt gracefully. The dashboard sidebar should collapse or convert to a mobile-friendly pattern. No horizontal overflow or clipped content.

### R3. Accessibility and SEO
All interactive elements must have accessible labels, proper ARIA roles, and keyboard navigation. Color contrast must meet WCAG AA. The landing page must have proper meta tags, semantic HTML, heading hierarchy, and structured content for search engine discoverability.

### R4. Performance and build quality
The production build must complete without TypeScript errors or warnings. Bundle size should be reasonable. Unused imports, dead code, and backup files (`.bak-*`, old HTML snapshots, loose JPEGs in the project root) should be cleaned up. The project structure should be tidy and organized.

### R5. Test coverage
All existing Playwright E2E tests must continue to pass. Add new tests covering: dashboard page navigation between all routes, mobile viewport rendering, and at least one accessibility check (e.g., no missing alt text, interactive elements have labels).

### R6. Copy and Text Optimization (Reducing Text Density)
Scan the landing page and dashboard content to reduce wordiness and density. Keep paragraph copy short and clean, headings bold and impactful, and layout spacious to ensure a premium, minimalist visual experience without text overload.

## Acceptance Criteria

### Build & Code Quality
- [ ] `npm run build` completes with zero errors and zero warnings
- [ ] No `.bak-*` backup files remain in `src/`
- [ ] No loose `.jpeg`/`.png` screenshot files remain in the project root (move to a dedicated folder or remove if unused)
- [ ] No unused imports across all `.tsx` files (verified by the TypeScript compiler's strict checks)

### Visual Quality & Content
- [ ] Landing page copy is condensed, clear, and high-impact (no wall of text)
- [ ] Landing page hero section displays the video background with visible contrast in both light and dark themes
- [ ] Dashboard pages render without visual glitches at 1440px, 1024px, 768px, and 375px viewport widths
- [ ] All modals (PolicyComposer, BiometricOverlay) open/close with smooth transitions
- [ ] Segmented controls, tab switches, and sidebar navigation animate smoothly

### Responsiveness
- [ ] At 375px viewport width, no horizontal scrollbar appears on any page
- [ ] Dashboard sidebar collapses or adapts on viewports below 768px
- [ ] Landing page sections stack vertically and remain readable on mobile

### Accessibility
- [ ] All `<img>` tags have non-empty `alt` attributes
- [ ] All `<button>` elements have accessible labels (visible text or `aria-label`)
- [ ] Toggle switches have associated labels
- [ ] Focus indicators are visible on interactive elements when tabbing with keyboard

### Tests
- [ ] All existing 22 Playwright tests pass (`npx playwright test`)
- [ ] At least 3 new tests added covering: dashboard route navigation, mobile viewport rendering, and one accessibility assertion
- [ ] Total test count ≥ 25
